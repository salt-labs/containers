# Run-time configuration file for dialog
# Automatically generated by "dialog --create-rc <file>"
# Types of values:                                              
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?,underline?,reverse?)

# Set aspect-ration.                                            
aspect = 0                                                                                                                      

# Set separator (for multiple widgets output).
separate_widget = ""                                            

# Set tab-length (for textbox tab-conversion).
tab_len = 1

# Make tab-traversal for checklist, etc., include the list.
visit_items = ON

# Shadow dialog boxeS? This also turns on color.
use_shadow = OFF

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (WHITE,WHITE,ON)

# Shadow color
shadow_color = (WHITE,DEFAULT,ON)

# Dialog box color
dialog_color = (WHITE,DEFAULT,OFF)

# Dialog box title color
title_color = (WHITE,RED,,OFF)

# Dialog box border color
border_color = (WHITE,DEFAULT,ON)

# Active button color
button_active_color = (DEFAULT,BLUE,OFF)

# Inactive button color
button_inactive_color = (WHITE,DEFAULT,OFF)

# Active button key color
button_key_active_color = (DEFAULT,BLUE,OFF)

# Inactive button key color
button_key_inactive_color = (DEFAULT,DEFAULT,OFF)

# Active button label color
button_label_active_color = (DEFAULT,BLUE,OFF)

# Inactive button label color
button_label_inactive_color = (WHITE,DEFAULT,OFF)

# Input box color
inputbox_color = (WHITE,BLACK,OFF)

# Input box border color
inputbox_border_color = (DEFAULT,DEFAULT,OFF)

# Search box color
searchbox_color = (WHITE,DEFAULT,OFF)

# Search box title color
searchbox_title_color = (DEFAULT,DEFAULT,OFF)

# Search box border color
searchbox_border_color = (WHITE,DEFAULT,OFF)

# File position indicator color
position_indicator_color = (DEFAULT,DEFAULT,OFF)

# Menu box color
menubox_color = (DEFAULT,DEFAULT,OFF)

# Menu box border color
menubox_border_color = (DEFAULT,DEFAULT,OFF)

# Item color
item_color = (WHITE,DEFAULT,ON)

# Selected item color
item_selected_color = (DEFAULT,BLUE,ON)

# Tag color
tag_color = (RED,DEFAULT,ON)

# Selected tag color
tag_selected_color = (DEFAULT,BLUE,ON)

# Tag key color
tag_key_color = (YELLOW,DEFAULT,OFF)

# Selected tag key color
tag_key_selected_color = (DEFAULT,BLUE,ON)

# Check box color
check_color = (WHITE,DEFAULT,OFF)

# Selected check box color
check_selected_color = (DEFAULT,DEFAULT,OFF)

# Up arrow color
uarrow_color = (GREEN,DEFAULT,OFF)

# Down arrow color
darrow_color = (GREEN,BLACK,OFF)

# Item help-text color
itemhelp_color = (WHITE,RED,OFF)

# Active form text color
form_active_text_color = (WHITE,BLUE,ON)

# Form text color
form_text_color = (YELLOW,BLACK,ON)

# Readonly form item color
form_item_readonly_color = (RED,DEFAULT,OFF)

# Dialog box gauge color
gauge_color = title_color

# Dialog box border2 color
border2_color = dialog_color

# Input box border2 color
inputbox_border2_color = dialog_color

# Search box border2 color
searchbox_border2_color = dialog_color

# Menu box border2 color
menubox_border2_color = dialog_color
